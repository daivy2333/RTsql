# M0 项目骨架与 Tokio 运行时实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize RTsql project skeleton with Tokio runtime and verify configuration through tests.

**Architecture:** Standard Rust project structure with lib.rs exporting five module placeholders (storage/executor/transaction/parser/network), main.rs as database server entry point with #[tokio::main], and runtime verification tests.

**Tech Stack:** Rust 2021 edition, Tokio (rt-multi-thread, macros, sync, time, net, fs features)

---

## File Structure

| File | Responsibility |
|------|----------------|
| `Cargo.toml` | Project config with Tokio dependency |
| `src/main.rs` | Database server entry point |
| `src/lib.rs` | Library entry, module exports |
| `src/storage/mod.rs` | Storage module placeholder |
| `src/executor/mod.rs` | Executor module placeholder |
| `src/transaction/mod.rs` | Transaction module placeholder |
| `src/parser/mod.rs` | Parser module placeholder |
| `src/network/mod.rs` | Network module placeholder |
| `tests/runtime_test.rs` | Runtime verification tests |

---

## Task 1: Initialize Rust Project

**Files:**
- Create: `Cargo.toml` (via cargo init)
- Create: `src/main.rs` (via cargo init)

- [ ] **Step 1: Run cargo init**

Run: `cargo init --name rtsql`
Expected: Creates Cargo.toml and src/main.rs

---

## Task 2: Configure Cargo.toml

**Files:**
- Modify: `Cargo.toml`

- [ ] **Step 1: Replace Cargo.toml with full configuration**

```toml
[package]
name = "rtsql"
version = "0.1.0"
edition = "2021"
description = "Async coroutine-driven embedded relational database"
license = "MIT OR Apache-2.0"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "net", "fs"] }

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true

[profile.dev]
opt-level = 0
```

- [ ] **Step 2: Verify Cargo.toml syntax**

Run: `cargo check`
Expected: No errors, downloads Tokio dependencies

---

## Task 3: Create Module Directory Structure

**Files:**
- Create: `src/storage/mod.rs`
- Create: `src/executor/mod.rs`
- Create: `src/transaction/mod.rs`
- Create: `src/parser/mod.rs`
- Create: `src/network/mod.rs`

- [ ] **Step 1: Create storage module**

Run: `mkdir -p src/storage`
Write: `src/storage/mod.rs`

```rust
//! Storage engine - Buffer pool, page cache, file I/O
//!
//! M1: Implement AsyncStorage trait and buffer pool
```

- [ ] **Step 2: Create executor module**

Run: `mkdir -p src/executor`
Write: `src/executor/mod.rs`

```rust
//! Execution engine - Physical plan execution, async iterator
//!
//! M5: Implement async fn next() -> Result<Option<Row>>
```

- [ ] **Step 3: Create transaction module**

Run: `mkdir -p src/transaction`
Write: `src/transaction/mod.rs`

```rust
//! Transaction management - MVCC, concurrency control
//!
//! M3: Implement transaction ID allocation and MVCC snapshot read
```

- [ ] **Step 4: Create parser module**

Run: `mkdir -p src/parser`
Write: `src/parser/mod.rs`

```rust
//! SQL parser - Parse SQL to internal representation
//!
//! M4: Integrate sqlparser-rs
```

- [ ] **Step 5: Create network module**

Run: `mkdir -p src/network`
Write: `src/network/mod.rs`

```rust
//! Network layer - TCP server, protocol implementation
//!
//! M6: Implement tokio::net::TcpListener and connection handling
```

---

## Task 4: Implement lib.rs

**Files:**
- Modify: `src/lib.rs` (create if not exists)

- [ ] **Step 1: Write lib.rs with module exports**

Write: `src/lib.rs`

```rust
//! RTsql library - Async embedded database components

pub mod storage;
pub mod executor;
pub mod transaction;
pub mod parser;
pub mod network;
```

- [ ] **Step 2: Verify module structure compiles**

Run: `cargo build`
Expected: Compiles successfully with no errors

---

## Task 5: Implement main.rs

**Files:**
- Modify: `src/main.rs`

- [ ] **Step 1: Replace main.rs with server entry point**

Write: `src/main.rs`

```rust
//! RTsql - Async coroutine-driven embedded relational database

use rtsql::storage;
use rtsql::executor;
use rtsql::transaction;
use rtsql::parser;
use rtsql::network;

#[tokio::main]
async fn main() {
    println!("RTsql database server starting...");

    // TODO: Initialize storage engine (M1)
    // TODO: Initialize execution engine (M5)
    // TODO: Start network server (M6)

    println!("RTsql ready. (M0 skeleton completed)");
}
```

- [ ] **Step 2: Verify main.rs compiles**

Run: `cargo build`
Expected: Compiles successfully, no unused import warnings (imports validate module exports work)

---

## Task 6: Write Runtime Verification Tests

**Files:**
- Create: `tests/runtime_test.rs`

- [ ] **Step 1: Create test directory**

Run: `mkdir -p tests`

- [ ] **Step 2: Write test_async_execution**

Write: `tests/runtime_test.rs` (part 1)

```rust
//! Runtime functionality test
//!
//! Verify Tokio runtime is properly configured:
//! - async function execution
//! - tokio::spawn task scheduling
//! - multi-thread scheduler behavior

#[tokio::test]
async fn test_async_execution() {
    let result = async_compute(42).await;
    assert_eq!(result, 84);
}

async fn async_compute(n: u32) -> u32 {
    n * 2
}
```

- [ ] **Step 3: Add test_spawn_task**

Append to `tests/runtime_test.rs`:

```rust
#[tokio::test]
async fn test_spawn_task() {
    let handle = tokio::spawn(async {
        100
    });

    let result = handle.await.expect("task should complete");
    assert_eq!(result, 100);
}
```

- [ ] **Step 4: Add test_multi_thread_spawn**

Append to `tests/runtime_test.rs`:

```rust
#[tokio::test]
async fn test_multi_thread_spawn() {
    use std::sync::Arc;
    use tokio::sync::Mutex;

    let counter = Arc::new(Mutex::new(0));
    let mut handles = vec![];

    for _ in 0..10 {
        let counter_clone = counter.clone();
        handles.push(tokio::spawn(async move {
            let mut guard = counter_clone.lock().await;
            *guard += 1;
        }));
    }

    for handle in handles {
        handle.await.expect("task should complete");
    }

    let final_count = *counter.lock().await;
    assert_eq!(final_count, 10);
}
```

---

## Task 7: Verify Build and Tests

**Files:**
- None (verification only)

- [ ] **Step 1: Run cargo build**

Run: `cargo build`
Expected: Compiles successfully, 0 errors, 0 warnings

- [ ] **Step 2: Run cargo test**

Run: `cargo test`
Expected: 3 tests pass, output shows "test result: ok. 3 passed; 0 failed"

- [ ] **Step 3: Run cargo clippy**

Run: `cargo clippy`
Expected: 0 warnings

---

## Task 8: Git Commit Skeleton Code

**Files:**
- None (git operations)

- [ ] **Step 1: Stage skeleton files**

Run: `git add Cargo.toml src/ tests/`
Expected: Files staged for commit

- [ ] **Step 2: Commit skeleton**

Run: `git commit -m "$(cat <<'EOF'
feat: Initialize RTsql project skeleton with Tokio runtime

M0 milestone completion:
- Cargo.toml with selective Tokio features
- src/lib.rs module exports
- src/main.rs server entry point with #[tokio::main]
- Placeholder modules: storage, executor, transaction, parser, network
- Runtime verification tests: async execution, spawn, multi-thread
EOF
)"`
Expected: Commit created successfully

- [ ] **Step 3: Verify git status**

Run: `git status`
Expected: "nothing to commit, working tree clean"

---

## Self-Review Checklist

After plan completion, verify:

- [x] **Spec coverage**: All spec sections covered by tasks
  - Cargo.toml → Task 2
  - main.rs → Task 5
  - lib.rs → Task 4
  - Modules → Task 3
  - Tests → Task 6
  - Verification → Task 7
  - Git → Task 8

- [x] **Placeholder scan**: No TBD/TODO/vague instructions (TODO in main.rs is design intent)

- [x] **Type consistency**: No cross-task type mismatches (no shared types in M0)

---

## Success Criteria

| Criterion | Verification |
|-----------|--------------|
| cargo build success | Task 7 Step 1 |
| cargo test 3 pass | Task 7 Step 2 |
| cargo clippy 0 warnings | Task 7 Step 3 |
| Git commit created | Task 8 Step 2 |
| Directory structure matches spec | Visual inspection after Task 5 |