/target
# Cargo.lock removed - binary applications should commit it for reproducible builds
**/*.rs.bk
*.pdb

# CodeGraph runtime data (SQLite index + daemon files)
/.codegraph/

# Added by cargo
# already existing elements were commented out

#/target
